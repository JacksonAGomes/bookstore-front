import { useState } from "react";
import './Client.css'

function Client() {
    return (
            <div className="buttons">
                <button onClick={()=>{
                }} id="bSave">Novo Cliente</button>
        </div>
    )
}
export default Client;